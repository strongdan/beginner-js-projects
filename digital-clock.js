const standardOrTwentyFourHour = () => {
  // prompts user for preference of clock format - 24hr or standard
}

const getCurrentTime = () => {
  // gets current time and returns it in datetime format
  let now = new Date();
  return now;
}

const displayClock = () => {
  // appends clock digits to main div in window
}

displayClock();
